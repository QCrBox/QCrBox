{#def commands = [] #}

<div class="block">
  {% if commands %}
  <div class="radios is-flex is-flex-direction-column">
    {% for command in commands %}
      <CommandsListItem command={{ command }} />
    {% endfor %}
    </div>
  {% else %}
    No commands registered.
  {% endif %}
</div>