QCRBOX_DOCKER_REPO=qcrbox
QCRBOX_DOCKER_TAG=latest
QCRBOXTOOLS_VERSION=0.0.5

QCRBOX_ROOT_DIR=/opt/qcrbox
QCRBOX_SUPERVISORD_CONF_DIR=${QCRBOX_ROOT_DIR}/supervisord_conf
QCRBOX_WINE_INSTALLATIONS_DIR="/opt/wine_installations"

QCRBOX_VOLUMES_ROOT_DIR=/mnt/qcrbox
QCRBOX_REGISTRY_DB_DIR=${QCRBOX_VOLUMES_ROOT_DIR}/qcrbox_registry_data/
QCRBOX_SHARED_FILES_DIR_CONTAINER_PATH=${QCRBOX_VOLUMES_ROOT_DIR}/shared_files/
QCRBOX_SHARED_FILES_DIR_HOST_PATH=./shared_files/

# By default, QCRBOX is only accessible on the same host, which makes
# local development more secure. In order to access QCRBOX remotely,
# the bind address should be set to "0.0.0.0".
#QCRBOX_BIND_ADDRESS="127.0.0.1"
QCRBOX_BIND_ADDRESS="0.0.0.0"

QCRBOX__REVERSE_PROXY__PORT=12345

# TLS certificates. Leave QCRBOX_TLS_CERT_RESOLVER empty to use Traefik's
# self-signed default certificate (local development). For a public deployment
# set it to "letsencrypt" and provide a contact email for Let's Encrypt.
QCRBOX_TLS_CERT_RESOLVER=
QCRBOX_ACME_EMAIL=

# NATS has no authentication and must never be exposed beyond this machine.
# The host port mapping below only serves local tooling (natscli, nats-top).
QCRBOX_NATS_BIND_ADDRESS=127.0.0.1

QCRBOX__NATS__HOST=localhost
QCRBOX__NATS__PORT=4222
QCRBOX__NATS__MANAGEMENT_PORT=8222
QCRBOX__NATS__STORE_DIR=.local_data/nats_store
QCRBOX_REGISTRY_HOST=localhost
QCRBOX_REGISTRY_PORT=11000
QCRBOX__REGISTRY__SERVER__ENABLE_AUTORELOAD=True


QCRBOX_SHELX_DOWNLOAD_USERNAME=CHANGEME-CHANGEME-CHANGEME
QCRBOX_SHELX_DOWNLOAD_PASSWORD=CHANGEME-CHANGEME-CHANGEME

# Domain configuration for the reverse proxy and Authelia.
# Browsers resolve any *.localhost name to 127.0.0.1 natively, so the default
# below needs no /etc/hosts entries (including from Windows for WSL users).
QCRBOX_DOMAIN=qcrbox.localhost

# Authelia Authentication Secrets
# DEVELOPMENT ONLY: these values are committed to the repository and therefore
# public. They must never be used on any machine reachable from a network.
# Generate new secrets with:
#   JWT: openssl rand -hex 32
#   Session: openssl rand -hex 64
#   Encryption: openssl rand -hex 32
AUTHELIA_JWT_SECRET=88d904c7a036b257435acb770af10a6626e60727c10d0b0d544c4cff926c5a5f
AUTHELIA_SESSION_SECRET=044b545ae4cd57e314b9c77e6d0b65b4b2bd5e87246f55531745ce024314881d9f31e57f83004df22102310904d19a06d78287ffdc0d62bb72c2b5c264dd4c88
AUTHELIA_STORAGE_ENCRYPTION_KEY=23cd0be9fa9ecb25eaa800adc7bbec2272df431de52e6c7849b6afdd883a6a8b

# LLDAP (user directory with web UI at https://users.<domain>)
# DEVELOPMENT ONLY values, same caveat as above. Generate new secrets with
# openssl rand -hex 32. The admin password is also the Authelia login for
# the 'admin' user.
LLDAP_JWT_SECRET=5b4f9b2d3f1a4f1d50ce20a08a3c2a59334a8e1cf0d3b9a6c54f7d1e8b20c4a7
LLDAP_KEY_SEED=9c6d1b3a7e2f4c5d8a90b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2
LLDAP_ADMIN_PASSWORD=changeme
