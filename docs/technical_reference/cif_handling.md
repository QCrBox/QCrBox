# Implementation of cif format transfer and handling

TODO:

- Description of `QCrBox.cif_data_file`, what it implements and how it's different to `QCrBox.data_file`
- How it's decided on when to transfer between cif formats
  - How is it implemented, e.g. it always tries to convert, where it lives in the code (e.g. method of the type)
- What is `QcrBox.output_cif`
- Transfer to specific format
- Transfer to unified format (e.g. which parameter is used and when)
